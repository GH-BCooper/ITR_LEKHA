import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Compare regimes', description: 'Old versus new income-tax regime for FY 2025-26, with every step of the working.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
