import Link from 'next/link';
import rules from '@/data/tax-rules/fy-2025-26.json';

export default function MethodologyPage() { return <><p className="eyebrow">Methodology</p><h1>The rules behind every figure</h1><p>FY {rules.financialYear}, AY {rules.assessmentYear}. Last verified: {rules.lastVerified}.</p><p className="muted">{rules.sourceNote}</p><h2>New-regime slabs</h2><table className="ledger"><thead><tr><th>Up to taxable income</th><th>Rate</th></tr></thead><tbody>{rules.newRegime.slabs.map((slab) => <tr key={slab.upTo ?? 'above'}><td>{slab.upTo ? `₹${slab.upTo.toLocaleString('en-IN')}` : 'Above ₹24,00,000'}</td><td>{slab.rate * 100}%</td></tr>)}</tbody></table><h2>What this tool does not handle</h2><ul><li>Business or professional income, including presumptive taxation.</li><li>Foreign income, foreign assets, DTAA or crypto/VDA.</li><li>Clubbing, revised or belated return rules, and advance-tax interest under 234A/B/C.</li><li>Real PDF parsing of Form 16 or AIS. Structured JSON is deliberate: silent extraction errors are unacceptable in a tax tool.</li></ul><p>Rates are loaded from the same versioned JSON used by the calculator. <Link href="/compare">Compare your figures</Link>.</p></>; }
