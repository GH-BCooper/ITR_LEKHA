import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Check Form 16 and AIS', description: 'Find mismatches between Form 16, AIS and your declared income before you file.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
